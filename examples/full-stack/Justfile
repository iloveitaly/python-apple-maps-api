set shell := ["zsh", "-ceuB", "-o", "pipefail"]
set ignore-comments := true

setup:
	mise install
	uv sync
	cd web && pnpm install

# Server API demo (geocode, search, autocomplete, …)
run:
	uv run python app/main.py

# API server + React UI (map + autocomplete)
dev:
	./run.sh
